[project]
name = "research-tools"
version = "0.1.0"
description = "Research plotting tools"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "matplotlib",
    "pandas",
    "seaborn",
]

[dependency-groups]
dev = []
